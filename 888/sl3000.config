# --- 1. Target System (物理目标定义) ---
CONFIG_TARGET_mediatek=y
CONFIG_TARGET_mediatek_filogic=y
CONFIG_TARGET_mediatek_filogic_DEVICE_ykm888_sl-3000=y

# --- 2. 引导零件工厂 (物理锁死 - 关键修复区) ---
# 开启基础包支持
CONFIG_PACKAGE_arm-trusted-firmware-mediatek=y
CONFIG_PACKAGE_uboot-mediatek=y

# 【核心修复】强制指定 DDR4 变体，与你的物理硬件对齐
CONFIG_PACKAGE_arm-trusted-firmware-mediatek-mt7981-nor-ddr4=y
CONFIG_PACKAGE_uboot-mediatek-mt7981-nor-ddr4=y

# 屏蔽其他不相关的引导变体，防止多余零件干扰 32MB 镜像合成
# CONFIG_PACKAGE_uboot-mediatek-mt7981-emmc is not set
# CONFIG_PACKAGE_uboot-mediatek-mt7981-sd is not set

# --- 3. 存储与底层逻辑 (5.15 内核 & EMMC 闭环) ---
CONFIG_LINUX_5_15=y
CONFIG_ZONE_DMA32=y
CONFIG_TARGET_ROOTFS_PARTSIZE=1024
CONFIG_TARGET_PER_DEVICE_ROOTFS=y
CONFIG_USE_SSTRIP=y

# EMMC 物理链路支持
CONFIG_PACKAGE_kmod-mmc=y
CONFIG_PACKAGE_kmod-mmc-mtk=y
CONFIG_PACKAGE_kmod-mtk-sd=y
CONFIG_PACKAGE_kmod-lib-crc32c=y
CONFIG_PACKAGE_block-mount=y

# 文件系统硬化
CONFIG_PACKAGE_kmod-fs-f2fs=y
CONFIG_PACKAGE_f2fs-tools=y
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_parted=y
CONFIG_PACKAGE_resize2fs=y

# --- 4. 网络与加速 (PPE 硬件卸载) ---
CONFIG_PACKAGE_kmod-mtk-eth-warp=y
CONFIG_PACKAGE_kmod-mtk-ppe=y
CONFIG_PACKAGE_kmod-mt7531=y
CONFIG_PACKAGE_luci-app-turboacc=y
CONFIG_PACKAGE_luci-app-turboacc-mtk=y

# --- 5. 无线驱动与 WPAD 冲突隔离 ---
CONFIG_PACKAGE_wpad-mesh-openssl=y
# CONFIG_PACKAGE_wpad is not set
# CONFIG_PACKAGE_wpad-basic is not set
# CONFIG_PACKAGE_wpa-supplicant is not set

# --- 6. UI、主题与应用插件 ---
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-app-easymesh=y
CONFIG_PACKAGE_luci-app-vssr=y
CONFIG_PACKAGE_luci-theme-argon=y

# --- 7. 构建环境硬化 (解决 ld-musl 路径漂移) ---
CONFIG_TOOLCHAIN_ROOT=""
CONFIG_EXTERNAL_TOOLCHAIN=n
CONFIG_IB=y
CONFIG_SDK=y

# --- 8. 物理产物优化 ---
CONFIG_TARGET_IMAGES_GZIP=y
CONFIG_TARGET_ROOTFS_SQUASHFS=y
# 确保生成包含引导零件的完整编程器镜像
CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_ykm888_sl-3000_SELECT_PROFILE=y
