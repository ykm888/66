name: SL3000-FullConfig-Fix-v3.7

on:
  workflow_dispatch:

jobs:
  fix-and-push:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: 1. 物理环境初始化
        run: |
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install build-essential flex bison g++ gawk gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev swig
          sudo timedatectl set-timezone "Asia/Shanghai"

      - name: 2. 物理克隆源码
        run: |
          git clone --depth 1 -b sl3000-padavan-2410 https://github.com/ykm888/66 openwrt

      - name: 3. 物理同步 Feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 4. 物理注入与架构强制锁定 (核心修复)
        run: |
          cd openwrt
          # 物理清空，拒绝旧配置干扰
          rm -f .config && touch .config
          
          # 层级 1：物理锁定 Mediatek 架构
          echo "CONFIG_TARGET_mediatek=y" >> .config
          echo "CONFIG_TARGET_mediatek_filogic=y" >> .config
          
          # 层级 2：【关键修复】物理删除 mt7622 干扰并锁定 mt7981
          # 确保系统不会偏移到 Bananapi
          echo "# CONFIG_TARGET_mediatek_mt7622 is not set" >> .config
          echo "CONFIG_TARGET_mediatek_mt7981=y" >> .config
          
          # 层级 3：锁定 SL-3000 设备 ID 及救砖包开关
          echo "CONFIG_TARGET_mediatek_mt7981_DEVICE_sl_3000-emmc=y" >> .config
          echo "CONFIG_TARGET_IMAGE_uboot_mediatek_mt7981_sl_3000_emmc=y" >> .config
          
          # 层级 4：物理屏蔽编译坏包（解决 Error 2）
          echo "# CONFIG_PACKAGE_datconf is not set" >> .config
          echo "# CONFIG_PACKAGE_rd05a1 is not set" >> .config
          
          # 执行物理对齐与膨胀
          make defconfig
          make oldconfig < /dev/null

      - name: 5. 物理准入校验
        run: |
          cd openwrt
          echo ">>> 检查物理架构（应为 mt7981）："
          grep "CONFIG_TARGET_mediatek_mt7981=y" .config
          echo ">>> 检查设备锁定（应为 sl_3000）："
          grep "sl_3000-emmc=y" .config
          echo ">>> 检查救砖包开关状态："
          grep "CONFIG_TARGET_IMAGE_uboot" .config

      - name: 6. 物理授权推送
        run: |
          git clone -b main https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/ykm888/66.git repo-main
          cp openwrt/.config repo-main/custom-config/sl3000.config
          cd repo-main
          git config --global user.email "bot@github.com"
          git config --global user.name "SL3000-Fix-Bot"
          git add custom-config/sl3000.config
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "chore: 物理锁定 MT7981 架构并补全全量救砖配置 [v3.7]"
            git push origin main
          fi
