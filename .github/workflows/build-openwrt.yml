name: SL3000 Final Fix

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/ykm888/66.git
  REPO_BRANCH: openwrt-24.10-6.6
  DIY_SH: 'scripts/sl3000-patch.sh'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Init
      run: |
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install $(curl -fsSL https://is.gd/depends_ubuntu_2204)

    - name: Clone
      run: git clone $REPO_URL -b $REPO_BRANCH openwrt

    - name: Patch & Generate Tool
      run: |
        chmod +x $DIY_SH
        cd openwrt && $GITHUB_WORKSPACE/$DIY_SH

    - name: Feeds
      run: cd openwrt && ./scripts/feeds update -a && ./scripts/feeds install -a

    - name: Config
      run: cd openwrt && make defconfig

    - name: Prepare Kernel & Run Atomic Fix
      run: |
        cd openwrt
        make target/linux/prepare -j$(nproc) V=s
        # 物理执行脚本生成的修复工具
        python3 kernel_fix.py

    - name: Final Build
      run: cd openwrt && make -j$(nproc) || make -j1 V=s

    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: SL3000-Firmware
        path: openwrt/bin/targets/mediatek/filogic/
